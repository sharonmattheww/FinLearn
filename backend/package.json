{
  "name": "finlearn-backend",
  "version": "1.0.0",
  "description": "FinLearn backend API server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "free-port": "kill-port 5050",
    "dev": "npm run free-port && node server.js",
    "dev:watch": "npm run free-port && nodemon server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.9.4"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.0"
  }
}
